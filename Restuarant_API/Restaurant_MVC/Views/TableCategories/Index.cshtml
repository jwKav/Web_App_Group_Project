@model IEnumerable<Restaurant_API.Models.TableCategory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TableCapacity)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TableCapacity)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TableCategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TableCategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TableCategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
